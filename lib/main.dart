import 'package:flutter/material.dart';
import 'package:firebase_core/firebase_core.dart';
import 'package:flutter_project_final/auth/login.dart';
import 'package:flutter_project_final/auth/signup.dart';
import 'package:flutter_project_final/screens/admin_dashboard.dart';
import 'package:flutter_project_final/screens/loanapplications.dart';
import 'package:flutter_project_final/screens/userhistory.dart';
import 'package:flutter_project_final/screens/userhome.dart';
import 'package:flutter_project_final/screens/userloan.dart';
import 'package:flutter_project_final/screens/userpayment.dart';
import 'package:flutter_project_final/screens/admin_history.dart';
import 'package:flutter_project_final/screens/userloanstatus.dart';
import 'firebase_options.dart'; // generated by FlutterFire CLI
import 'theme/app_theme.dart';

const bool newUiEnabled = true; // set to false to keep old UI

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Midas Touch',
      debugShowCheckedModeBanner: false,
      theme: newUiEnabled ? AppTheme.theme() : ThemeData(
        useMaterial3: true,
        colorScheme: ColorScheme.fromSeed(
          seedColor: Colors.green,
          brightness: Brightness.light,
        ),
        cardTheme: const CardThemeData(
          elevation: 4,
          shape: RoundedRectangleBorder(
            borderRadius: BorderRadius.all(Radius.circular(12)),
          ),
        ),
        elevatedButtonTheme: ElevatedButtonThemeData(
          style: ElevatedButton.styleFrom(
            padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 12),
            shape: RoundedRectangleBorder(
              borderRadius: BorderRadius.circular(12),
            ),
          ),
        ),
        appBarTheme: const AppBarTheme(centerTitle: true, elevation: 2),
      ),
      initialRoute: '/login',
      routes: {
        '/login': (_) => const LoginScreen(),
        '/signup': (_) => const SignupScreen(),
        '/userDashboard': (_) => const HomeScreen(), // 👈 for normal users
        '/adminDashboard': (_) => const AdminDashboard(), // 👈 for admins
        '/userloan': (_) => const LoanPage(),
        '/userpayment': (_) => const PaymentPage(),
        '/userhistory': (_) => const UserHistoryPage(),
        '/LoanApplicationsPage': (_) => const LoanApplicationsPage(),
        '/adminHistory': (_) => const AdminHistoryPage(),
        '/userloanstatus': (_) => const UserLoanStatusPage(),
      },
      onGenerateRoute: (settings) {
        // Handle navigation for pages that might be accessed directly via URL
        switch (settings.name) {
          case '/userDashboard':
            return MaterialPageRoute(builder: (_) => const HomeScreen());
          case '/adminDashboard':
            return MaterialPageRoute(builder: (_) => const AdminDashboard());
          case '/userloan':
            return MaterialPageRoute(builder: (_) => const LoanPage());
          case '/userpayment':
            return MaterialPageRoute(builder: (_) => const PaymentPage());
          case '/userhistory':
            return MaterialPageRoute(builder: (_) => const UserHistoryPage());
          case '/LoanApplicationsPage':
            return MaterialPageRoute(
              builder: (_) => const LoanApplicationsPage(),
            );
          case '/adminHistory':
            return MaterialPageRoute(builder: (_) => const AdminHistoryPage());
          case '/userloanstatus':
            return MaterialPageRoute(
              builder: (_) => const UserLoanStatusPage(),
            );
          default:
            return null;
        }
      },
    );
  }
}
